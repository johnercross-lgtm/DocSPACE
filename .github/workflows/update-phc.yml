name: Update PHC News Feed

on:
  workflow_dispatch:
  schedule:
    - cron: "20 */6 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Generate PHC feed
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ vars.OPENAI_MODEL || 'gpt-4.1-nano' }}
        run: python scripts/fetch_phc.py

      - name: Commit PHC feed
        run: |
          if [ ! -f data/phc-feed.json ]; then
            echo "data/phc-feed.json was not created"
            exit 1
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/phc-feed.json

          if git diff --cached --quiet; then
            echo "No PHC feed changes"
          else
            git commit -m "Update PHC news feed"
            git push
          fi
